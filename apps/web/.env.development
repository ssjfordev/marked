# ============================================================
# Development Environment (local dev + Vercel marked-dev)
# Overrides .env.common values where needed
# Secrets (API keys, etc.) are set in .env.local or Vercel
# ============================================================

# Environment Mode
ENV=development

# App URL
NEXT_PUBLIC_APP_URL=https://marked-web-eight.vercel.app/
