# Production environment
# These should be set in your deployment platform (Vercel, etc.)

# NEVER enable DEV_AUTH_BYPASS in production
DEV_AUTH_BYPASS=false

# App URL (update to your production domain)
NEXT_PUBLIC_APP_URL=https://marked.example.com
